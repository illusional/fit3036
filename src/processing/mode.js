export default {
    include: 'include',             // Include the whole road 
    intersect: 'intersect',   // Intersect the road at the border
    truncate: 'truncate'            // Truncate the road to the nearest intersection
};